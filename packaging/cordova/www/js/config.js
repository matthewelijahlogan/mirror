/* Cordova packaging config
   Set `API_BASE_URL` to the full URL of your running backend (including https://).
   Example: const API_BASE_URL = 'https://abc123.ngrok.io';
*/

const API_BASE_URL = ""; // <-- set this before building/running the app
